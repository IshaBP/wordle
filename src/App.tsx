import { useState } from "react";
import { Keyboard } from "./components/keyboard/Keyboard";

function App() {
  return (
    <main className="App">
      <Keyboard onKey={() => {}} />
    </main>
  );
}

export default App;
